    //-------------------------------------------------------------------------

    if (seccomp_load(ctx) != 0) {
        rc = 2;
        goto out;
    }

    return rc;

out:
    seccomp_release(ctx);
    return rc;
}

int main()
{
    setup_seccomp_whitelist();

    // Put your code below
    return 0;

}